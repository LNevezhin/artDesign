/*           setArray: function () {
                  let projectSub = [];
                  let key = 0;
                  let projectSubMenu = [];
                  let projIndex = 0;
                  let position = 0;
                  for (projIndex = 0; projIndex < this.apidata.sliderBlock.length;) {
                    for (position = 0; position < this.apidata.sliderBlock[this.projIndex].types.length;) {
                      for (let i = 0; i < 3; i++) {
                        key = key + this.apidata.sliderBlock[this.projIndex].types[position][i].toLowerCase();
                      }
                      for (let i = 0; i < this.apidata.sliderBlock[this.projIndex].images.length; i++) {
                        if (this.apidata.sliderBlock[this.projIndex].images[i].toLowerCase().indexOf(key) != -1) {
                          projectSub[position].push(i);
                        }
                      }
                    }
                    projectSubMenu[projIndex] = projectSub;
                  }
                  console.log('projectSubMenu: ', projectSubMenu);
                  return console.log(projectSubMenu);
                }, */